services:
  backend:
    build: .
    container_name: plutoploy-backend
    restart: unless-stopped
    env_file: .env
    environment:
      PORT: "3000"
    ports:
      - "${HOST_PORT:-3000}:3000"
