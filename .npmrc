# Slow/flaky registry hardening — pnpm gives up too early by default.
# Fewer parallel sockets each get more of a thin pipe; longer timeouts ride out stalls.
fetch-retries=5
fetch-retry-mintimeout=20000
fetch-retry-maxtimeout=120000
fetch-timeout=600000
network-concurrency=4
